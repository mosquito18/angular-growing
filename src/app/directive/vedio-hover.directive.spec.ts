import { VedioHoverDirective } from './vedio-hover.directive';

describe('VedioHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new VedioHoverDirective();
    expect(directive).toBeTruthy();
  });
});
