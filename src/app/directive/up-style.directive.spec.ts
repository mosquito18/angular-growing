import { UpStyleDirective } from './up-style.directive';

describe('UpStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new UpStyleDirective();
    expect(directive).toBeTruthy();
  });
});
